#include "public.h"

//
//组内内部排序, 然后看待组为整体, 未归属的属于只有一个元素的组, 再外部排序
//就是拓扑排序
//来不及写了

class Solution {
public:
	vector<int> sortItems(int n, int m, vector<int>& group, vector<vector<int>>& beforeItems) {
		//
	}
};
