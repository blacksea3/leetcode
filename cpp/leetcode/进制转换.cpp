#include <iostream>
#include <fstream>
#include <string>

using namespace std;

/*
int main()
{
	{
		string s;
		while (cin >> s)
		{
			int res = 0;
			int index = 2;
			while (index < s.size())
			{
				if (isdigit(s[index]))
				{
					res *= 16;
					res += s[index] - '0';
				}
				else
				{
					res *= 16;
					res += s[index] - 'A' + 10;
				}
			}
			cout << res << endl;
		}
	}
}
*/
